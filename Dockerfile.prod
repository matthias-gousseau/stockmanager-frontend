# Étape 1 : Base Node slim pour installer les dépendances
FROM node:22-slim AS base

WORKDIR /app

# Copier uniquement package.json et package-lock.json pour optimiser le cache
COPY package*.json ./

# Installer les dépendances
RUN npm ci --legacy-peer-deps

# Copier uniquement la build prod
COPY stockmanager-frontend/dist/stockmanager-frontend /app

# Exposer le port Angular prod server
EXPOSE 4200

# Lancer le serveur Angular en prod
CMD ["serve", "-s", ".", "-l", "4200"]